#include <stdio.h>
#include <stdlib.h>

/**
 * main - main block
 * Description: Prints the alphabet in lowercase
 * Return: 0
 */

int main(void) 
{
  char alpha;

  for(alpha = 'a'; alpaha <= 'z'; alpha++)
      putchar(alpha);

   putchar('\n');
   return (0);  
}
