#include <stdio.h>
#include <stdlib.h>

/**
 * main - main block
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */

int main(void) 
{
  char alphabet = 'a';

  while (alphabet <= 'z')
  {
    // print the value of the variable	  
    putchar(c);
    // increment the variabble (ie: a becomes b)
    c++
  }

  putchar('\n');
  return(0);
}
